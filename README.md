To-Do list

In submit preview, have checkboxes to include Tech by default, user can uncheck 2nd/3rd tech to not include them if they want.

On Admin -> system Health, if parent DB is anything other than "Connected" Show button to restart the API via a button. And a test button. 

Tower View, Only be allowed to ping one sector at a time. If pings are running on a sector and a tech tries to start pings on another, stop the pings on the first sector. 

Ensure when loading a new Navigation Pane on Dispatch, that the pane refreshes upon clicking. 

Really take a look at the ticket carry-over rule and ensure that tickets will carry over. Unable to test while app is in production. 


Done
Lineman make Site History Copy-able - Done

Need a skittle on the Dispatch Tasks. (Notification Bubble) - Done

Anyway to implement spell check on the write up block? - Done

Dispatch -> Daily Assignments, grey out techs that are unavailable or supposed to be scheduled "off". Done
