<UserControl x:Class="SmartGridSuite.Client.Views.Dispatcher.Panes.SiteHistoryPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="SiteHistoryGridTextStyle"
           TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Margin" Value="10,0"/>
        </Style>

        <Style x:Key="SiteHistoryGridIssueTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SiteHistoryGridTextStyle}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="MaxHeight" Value="44"/>
        </Style>

        <Style x:Key="SiteHistoryDetailLabelStyle"
           TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        </Style>

        <Style x:Key="SiteHistoryDetailTextBoxStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource ModernTextBox}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="SiteHistoryIssueTextBoxStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource ModernTextBox}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="SiteHistoryBodyTextBoxStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource ModernTextBox}">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="MinHeight" Value="260"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="DangerButtonStyle"
       TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="40"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="BorderBrush" Value="#B91C1C"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd"
                                Property="Background"
                                Value="#B91C1C"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd"
                                Property="Background"
                                Value="#991B1B"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header card only -->
        <Border Grid.Row="0"
                Style="{StaticResource Card}"
                Padding="16">
            <StackPanel>
                <TextBlock Text="Site History"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>

                <TextBlock Text="Search a site, review history, and correct SmartGridSuite write-ups."
                           Margin="0,4,0,0"
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.15*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="1.35*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Search + history grid -->
            <Border Grid.Column="0"
                    Style="{StaticResource Card}"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0"
                          HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="260"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="SiteSearchTextBox"
                                 Grid.Column="0"
                                 Height="34"
                                 VerticalContentAlignment="Center"
                                 Padding="10,0"
                                 Tag="Type in a Site #"
                                 Style="{StaticResource ModernWatermarkTextBox}"
                                 KeyDown="SiteSearchTextBox_KeyDown"/>

                        <Button Grid.Column="2"
                                Content="Search"
                                Width="90"
                                Height="34"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="Search_Click"/>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="ResultsHeaderTextBlock"
                                   Text="Search for a site"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"/>

                        <TextBlock x:Name="ResultsCountTextBlock"
                                   Grid.Column="1"
                                   Foreground="{DynamicResource TextSecondary}"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <DataGrid x:Name="HistoryGrid"
                              Grid.Row="4"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              RowHeaderWidth="0"
                              RowHeight="46"
                              MinRowHeight="46"
                              FontSize="14"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ItemsSource="{Binding HistoryRows}"
                              SelectionChanged="HistoryGrid_SelectionChanged"
                              Style="{StaticResource ThemedDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Date / Time"
                                                    Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DateTimeText}"
                                                   Style="{StaticResource SiteHistoryGridTextStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Tech 1"
                                                    Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Tech1Text}"
                                                   Style="{StaticResource SiteHistoryGridTextStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Tech 2"
                                                    Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Tech2Text}"
                                                   Style="{StaticResource SiteHistoryGridTextStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Issue"
                                                    Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding IssueText}"
                                                   Style="{StaticResource SiteHistoryGridIssueTextStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="8"
                          Background="Transparent"
                          HorizontalAlignment="Stretch"/>

            <!-- RIGHT: Selected history details -->
            <Border Grid.Column="2"
                    Style="{StaticResource Card}"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Selected Write-Up"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimary}"/>

                            <TextBlock x:Name="EditAuditTextBlock"
                                       Margin="0,2,0,0"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextSecondary}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Top">
                            <Button x:Name="EditButton"
                                    Content="Edit"
                                    Width="90"
                                    Height="32"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="Edit_Click"
                                    IsEnabled="False"/>

                            <Button x:Name="DeleteButton"
                                    Content="Delete"
                                    Width="90"
                                    Height="32"
                                    Style="{StaticResource DangerButtonStyle}"
                                    Click="Delete_Click"
                                    IsEnabled="False"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock x:Name="SelectedHintTextBlock"
                               Grid.Row="2"
                               Text="Select a history row to view it."
                               Foreground="{DynamicResource TextSecondary}"
                               TextWrapping="Wrap"/>

                    <!-- Auto-filled fields -->
                    <Grid Grid.Row="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0"
                                    Grid.Column="0">
                            <TextBlock Text="Date / Time"
                                       Style="{StaticResource SiteHistoryDetailLabelStyle}"/>

                            <TextBox x:Name="DateTimeTextBox"
                                     Margin="0,4,0,0"
                                     Style="{StaticResource SiteHistoryDetailTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0"
                                    Grid.Column="2">
                            <TextBlock Text="Primary Tech"
                                       Style="{StaticResource SiteHistoryDetailLabelStyle}"/>

                            <ComboBox x:Name="Tech1ComboBox"
                                      Margin="0,4,0,0"
                                      Width="Auto"
                                      MinWidth="0"
                                      HorizontalAlignment="Stretch"
                                      ItemsSource="{Binding TechnicianOptions}"
                                      Style="{StaticResource ModernComboBox}"
                                      SelectionChanged="DetailComboBox_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="3">
                            <TextBlock Text="Secondary Tech / Crew"
                                       Style="{StaticResource SiteHistoryDetailLabelStyle}"/>

                            <ComboBox x:Name="Tech2ComboBox"
                                      Margin="0,4,0,0"
                                      Width="Auto"
                                      MinWidth="0"
                                      HorizontalAlignment="Stretch"
                                      ItemsSource="{Binding TechnicianOptions}"
                                      Style="{StaticResource ModernComboBox}"
                                      SelectionChanged="DetailComboBox_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Row="4"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="3">
                            <TextBlock Text="Issue"
                                       Style="{StaticResource SiteHistoryDetailLabelStyle}"/>

                            <TextBox x:Name="IssueTextBox"
                                     Margin="0,4,0,0"
                                     TextChanged="DetailTextBox_TextChanged"
                                     Style="{StaticResource SiteHistoryIssueTextBoxStyle}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Grid.Row="6"
                   Text="Write-Up Body"
                   Style="{StaticResource SiteHistoryDetailLabelStyle}"/>

                    <TextBox x:Name="NarrativeTextBox"
                             Grid.Row="8"
                             Height="Auto"
                             TextChanged="DetailTextBox_TextChanged"
                             Style="{StaticResource SiteHistoryBodyTextBoxStyle}"/>

                    <StackPanel Grid.Row="10"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button x:Name="CancelButton"
                                Content="Cancel"
                                Width="90"
                                Height="32"
                                Margin="0,0,8,0"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="Cancel_Click"
                                IsEnabled="False"/>

                        <Button x:Name="SaveButton"
                                Content="Save"
                                Width="90"
                                Height="32"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="Save_Click"
                                IsEnabled="False"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>